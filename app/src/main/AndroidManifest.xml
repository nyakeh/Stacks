<?xml version="1.0" encoding="utf-8"?>
<manifest package="uk.co.nyakeh.stacks"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".StockActivity"
            android:label="@string/title_activity_stock">
        </activity>
        <activity
            android:name=".StockPurchaseActivity"
            android:label="@string/title_activity_stock_purchase"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="uk.co.nyakeh.stacks.MainActivity"/>
        </activity>
    </application>

</manifest>
